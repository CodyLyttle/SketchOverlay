<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:SketchOverlay.Views"
             xmlns:library="clr-namespace:SketchOverlay.LibraryAdapters"
             xmlns:controls="clr-namespace:SketchOverlay.Controls"
             x:Class="SketchOverlay.Views.MainPage"
             x:DataType="library:MauiOverlayWindowViewModel">
    <Grid>
        <controls:WindowsGraphicsView x:Name="graphicsView" 
                                 MouseDownCommand="{Binding MouseDownCommand}"
                                 MouseDragCommand="{Binding MouseDragCommand}"
                                 MouseUpCommand="{Binding MouseUpCommand}"/>

        <views:DrawingToolWindow x:Name="DrawingToolsWindow"/>
    </Grid>
</ContentPage>
